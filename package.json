{
  "name": "fakestore-cart-api-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright API test suite for fakestoreapi.com Cart endpoints",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --reporter=list",
    "test:ci": "CI=1 playwright test",
    "report": "playwright show-report",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "typescript": "^5.7.2",
    "zod": "^3.24.1"
  }
}
